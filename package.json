{
  "name": "carmens-laundry-service",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler pages deploy . --project-name=carmenlaundry-website",
    "secret-bulk": "wrangler pages secret bulk"
  },
  "devDependencies": {
    "wrangler": "latest"
  }
}
