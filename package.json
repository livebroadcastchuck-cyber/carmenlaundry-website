{
  "name": "carmens-laundry-service",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "mkdir -p dist && cp index.html index.js styles.css dist && cp -r images dist",
    "deploy": "npm run build && wrangler pages deploy dist --project-name=carmenlaundry-website",
    "secret-bulk": "wrangler pages secret bulk"
  },
  "devDependencies": {
    "wrangler": "latest"
  }
}
